{
  "name": "threshold-staking-bonus",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ threshold-staking-bonus",
    "create-local": "graph create --node http://localhost:8020/ threshold-staking-bonus",
    "remove-local": "graph remove --node http://localhost:8020/ threshold-staking-bonus",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 threshold-staking-bonus"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
